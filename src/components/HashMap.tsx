import React from 'react';

function HashMap() {
    const map = new Map<number, number>();
    map.set(1, 1);
    return (
        <div>
            <h1>HashMap</h1>
        </div>
    );
}

export default HashMap;
